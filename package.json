{
  "name": "rent-app",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone rent-a-shop marketing/signup site for LilTeam Shop — talks to the main app only through its internal API, never touches MongoDB directly.",
  "main": "server.js",
  "scripts": {
    "build:css": "tailwindcss -i ./public/css/tailwind.source.css -o ./public/css/tailwind.generated.css --minify",
    "test": "node scripts/smoke-server.js",
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "bcryptjs": "^3.0.3",
    "connect-flash": "^0.1.1",
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "express-async-errors": "^3.1.1",
    "express-session": "^1.18.1",
    "form-data": "^4.0.1",
    "multer": "^2.3.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  }
}
